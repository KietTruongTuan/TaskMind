services:
  app:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
      - /app/BE/__pycache__
    ports:
      - "3000:3000"
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DEBUG=1
      - NEXT_PUBLIC_API_URL=http://localhost:8000