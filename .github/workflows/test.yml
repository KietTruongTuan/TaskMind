name: Test FE
on: 
  pull_request:
    branches:
      - dev
jobs:
  unit_test:
    runs-on: ubuntu-latest
    container:
      image: node:22
    steps:
      - uses: actions/checkout@v3
      - run: npm install -g pnpm
      - run: pnpm install
      - run: pnpm test:unit
